# content-brief

GenGrowth 内容链（Content Brief Builder / Content Draft Writer）的纯领域模块。只依赖本目录与 `@sf/public-tools` 内部；不 import apps/*，不含时钟、随机与网络。营销站只通过 `package.json` 的 `./content-brief/*` 子路径 import。

| 文件 | 职责 |
|---|---|
| `contract.ts` | 内容链数据契约 v1：ContentBrief / DraftResult 类型、封闭枚举、handoff 常量与 API 错误码（只读，改动 = 合同变更） |
| `constants.ts` | 引擎全部阈值、预算、配额与语言表（STOPWORDS / PRESERVED_QUESTION_PREFIXES）；UI 与测试只从这里读数字 |
| `canonical.ts` | `canonicalize`（键按 UTF-16 码元排序、跳过 undefined 的稳定序列化）、`sha256Hex`（WebCrypto 优先、node:crypto 动态回退）、`briefFingerprint` / `draftFingerprint`（去掉 run.fingerprint 与 run.elapsed_ms 后哈希，不改入参） |
| `canonical.test.ts` | 键序 / undefined / 嵌套 / 数字与字符串转义 / 两条哈希路径对照向量 / 指纹对易变字段不敏感、对其它字段敏感 / 不可变 |
| `cluster.ts` | handoff §4.5 步骤 2–5：`normalizeHeading`（序号、品牌 token、停用词、问句前缀保留、截断）、`clusterHeadings`（(rank, 原文顺序) 遍历的单链接词法聚类，Jaccard 或 token 边界子串并簇）、`selectMustAnswer`（门槛、排序、CAP、候选 / 隐藏计数） |
| `cluster.test.ts` | 归一化各规则、同义并簇 / 不并簇、同页只计一次 covered_by、代表串、first_rank、确定性、空表语言、门槛与截断、不可变 |
| `classify.ts` | handoff §4.6 有序规则表：四个域名集合、`FORMAT_RULES` / `classifySerpFormat`（域名 → 路径 → 标题，首条命中即 value，全部命中进 rules_hit）、`INTENT_RULES` / `classifyIntent`（navigational 优先，多数决，平局取 rank 最靠前的领先意图并标 tie） |
| `classify.test.ts` | 每条规则一个用例、顺序优先级、unknown、www./子域后缀匹配、平局与非平局、无命中返回 null、规则表顺序钉死 |
| `assemble.ts` | 由各路 reads 与证据派生 ContentBrief 全部字段并盖指纹（并行任务产出，职责以其文件头为准） |
| `assemble.test.ts` | assemble 的契约真值表测试（并行任务产出） |
| `parse-brief-shape.ts` | parser 的 decoder 组合子（已 export 供 parse-draft.ts 复用；`object` 精确键集 / `tagged` 判别联合 / `array` 上限与唯一 / `identifier` / `httpUrl`…，既校验又重建）与全部逐字段形状钉：字节上限、schema 字面量、枚举、长度（模型文本读 `MODEL_TEXT_MAX_CHARS` / `QUESTION_MAX_CHARS`）、URL、id 模式、`SerpReadMeta.unresolved`、`GscReadMeta.rows`、每页 h2/h3 ≤ `CRAWL_HEADINGS_PER_PAGE_MAX`、模型自由文本走 text.ts `isBoundedModelText`（码点计长、非空、无控制符/尖括号）；导出 `decodeBriefShape` / `handoffEnvelope` / `ParseBriefFailure` |
| `parse-brief.ts` | handoff §5.1 的唯一 exact parser：形状（parse-brief-shape.ts）之后**用 assemble.ts 同一套函数重算再逐键比对**——`buildSerpObservations`（逐行重分类，value 与有序 rules_hit）、`buildCrawlReadMeta`、`buildIntentField` / `buildFormatField` / `buildLengthField`、`buildMustAnswerDraft`（簇骨架 + candidates/shown/hidden，只放行模型可改的 q / q_provenance）、`modelDerivedFrom`（每个 method:"model" 的 derived_from）、`applyModelOutput` 的四道 unavailable 闸门（含 llm 失败原因透传）、`buildDraftReadiness`、`deriveBriefRunMode`、`deriveVerdictFromLedger`（整份 verdict 走 verdict.ts 同一张决策表，`MIN_DIMENSION_COVERAGE` 取自 gsc-analytics/page-reader）、`planCrawlTargets(serp, hostKey)`（skipped 列表顺序与内容）；另钉 `gsc_query_page` 每行 normalizeQuery === 主词且 (query,page) 唯一、`reads.gsc.rows` 为账本分母、outline 覆盖全部 Q 恰一次且 id 为 O1..On、gap_angle 在 observed 为 0 时 insufficient_evidence(0)、handoff `created_at <= now < expires_at`（`deps.now` 可注入）；Unavailable 分支同样 exact compare；不等 → `brief_reference_invalid` + 首个不同键的路径。crawl 只在 SERP 不可得（builder started=false）或预算前置耗尽（`timeout/0`）时允许 Unavailable，全 skipped 走 available/complete。`parseContentBriefShape`（同步，不算指纹）/ `parseContentBrief`（再重算 `briefFingerprint`，可注入）/ `parseContentBriefHandoff`（envelope + TTL 钉，嵌套路径带 `brief.`） |
| `parse-brief.test.ts` | 形状拒绝 + 每条重算的「改了自报值 / 改了账本不重算」反例（改 title 不改 format、伪造/重排 rules_hit、改 intent value/matched/confidence、删合格簇、挪簇成员、改页标题、伪造 derived_from、Unavailable 的 reason/attempted 错一位…）+ 九个经 builder 组装的正例变体（SERP 不可得 / 全 skipped / 预算前置超时 / LLM 校验失败 / zh / 篇幅可得 / 主词未观测 / 接通 / 接通且 LLM 失败）+ 深拷贝 + 注入指纹与真 `briefFingerprint`（含「重算指纹后仍被抓」）+ handoff |
| `fixtures.ts` | `contentBriefFixture(options)` 经 assemble.ts 同一套 builder 组装（knob：serp / crawlTimeout / allSkipped / llm / language / completeC5 / connected / notObserved）；接通变体的判定与 `gsc_query_page` / `matched_queries` / `primary_coverage` 由 `computeVerdict` 从原始 GSC 行算出；`validContentBrief(overrides?)`、`validConnectedContentBrief()`、`withFingerprint()`；页标题刻意跨页重复以让词法聚类产出 4 个合格簇；DeepPartial 合并对数组整体替换 |
- `verdict.ts` — `computeVerdict({ primary, queryRows, queryPageRows, queryPagingTruncated, queryUnreadableRows, coverageOf, minDimensionCoverage })`: the self-competition rule on the primary keyword only. Normalises the reader's position-0 sentinel to null, merges spelling variants per page (impression-weighted position), and returns the three-state verdict plus the G/query×page ledger rows and `primary_coverage`; `verdict.test.ts` covers every branch.
| `validate-section.ts` | handoff §5.3 的唯一句级校验器 `validateSectionOutput(output, { citableCrawlIds, profileFacts })`：句数 ≤ `SECTION_MAX_SENTENCES`、句文本走 `boundedModelText(SENTENCE_MAX_CHARS)`、claim 封闭枚举、`bound` 必有 refs 且 C* 必须带 excerpt、P* 不能是 inferred；`stance` 只能引 P*；`gap`/`no_claim` refs 必空；ref 唯一；`support_count` 由 C* 数派生；不改写任何 claim（整段失败让 handler 重试或标 `validation_failed`）；`countWords` 是 LengthField 同一把空白分词器 |
| `validate-section.test.ts` | 每条规则一正一反 + 句数上限 + 不变输入 |
| `draft-assemble.ts` | DraftResult 的纯组装：`planSections`（按 outline 顺序切 requested/skipped，非 writable → `section_not_writable`）、`gapAngleSectionId`（outline 最后一节）、`deriveVerifyList` / `deriveTotals` / `deriveSectionReads` / `aggregateSectionLlm` / `deriveDraftRunMode`（§5.7 真值表）、`decideCoverage`（失败/跳过段的 Q 服务端直接判 none + cause，其余交模型）、`validateCoverageOutput`、`buildCoverage`、`assembleDraftResult`（算 `draftFingerprint`） |
| `draft-assemble.test.ts` | planSections 顺序与拒绝、verify/reads/mode/aggregate 派生、coverage 三段、组装后指纹可重算 |

| `parse-draft.ts` | 工具二的唯一 exact parser（decoder 组合子、`unavailableShape` / `llmReadMeta` 从 parse-brief-shape.ts 导入，`recomputed` / `sameSet` 从 parse-brief.ts 导入，不留副本）：形状与上限（每段 JSON ≤ `SECTION_BODY_MAX_BYTES`、句数 ≤ `SECTION_MAX_SENTENCES`、句长 / h2 / coverage gap 走 `isBoundedModelText`、attempts ≤ `SECTION_MAX_ATTEMPTS`、`budget_ms` 只认两档、各 union 分支键集耦合、无未知键、返回新对象）+ failed 段 attempts 交叉约束（not_configured ⇒ 0 且 tokens null；validation_failed / provider_error ⇒ ≥ 1；attempts 0 ⇒ tokens null）之后**用 draft-assemble.ts / validate-section.ts 同一套函数重算再逐键比对**：每段 body 还原成 `ModelSectionOutput` 喂 `validateSectionOutput`——有 brief 时证据按段取 `sectionEvidenceFor(brief, section.id, settings)`（= `sectionEvidenceScope`：只有该段 answers 对应簇成员里带 excerpts 的 C*，P* 按 product_mention：none ∅ / gap_only 只给缺口角度那一节 gap_angle.profile_fact_refs / throughout 全部，`stanceAllowed` 只在缺口角度那一节为真），无 brief 时对引用取信只查 claim 规则与 support_count / word_count；`deriveVerifyList` / `deriveTotals` / `deriveSectionReads`（且 requested ≥ 1）/ `deriveDraftRunMode`；首跑 `aggregateSectionLlm` 子集（status / calls / tokens / failed_reasons / reason / attempted，沿用「真实 attempt 无 usage ⇒ 聚合 null」）；重跑（`reran_from` 非 null ⇒ `SECTION_ENDPOINT_BUDGET_MS`）无凭据时弱钉单次调用一致性，`deps.rerun = { previousRunId, sectionId, call }` 时钉 `reran_from`、目标段 status / fail_reason / attempts / tokens 与 call 完全一致、`llm_sections === aggregateSectionLlm([call], call.temperature_requested)` exact；`deps.coverageLlm` 时钉 `run.reads.llm_coverage` exact；coverage 本地（owner 段 failed/skipped ⇔ heuristic 与 cause、covered_in 只指 ok 段、四计数、有 model 项 ⇒ llm_coverage complete 且 calls 1 且 temperature_requested 0）与绑定 brief 后 `decideCoverage` + `buildCoverage` 整体 exact compare 再查账本（askable 为空 ⇒ llm_coverage 整体 === { unavailable, insufficient_evidence, attempted 0, calls 0, model_id null, tokens null }；askable 非空且 coverage unavailable ⇒ reason 不得是 not_requested / quota_exhausted / unsupported_language / insufficient_evidence，not_configured ⇒ attempted 0 / calls 0，timeout ⇒ 1/1 或 0/0，其余 attempted === calls，calls 0 ⇒ model_id 与 tokens 全 null，llm complete（validation_failed）⇒ calls 1 且 temperature 0）；`brief_ref` === brief 的 run_id / fingerprint / keyword；sections === `planSections(writable)` 的 id 顺序 / h2 / answers（先于正文判定）。`parseDraftResultShape`（同步，不带 brief，不算指纹）/ `parseDraftResult(input, brief, deps?)`（再重算 `draftFingerprint`，可注入）/ `parseDraftSections(input, brief, settings)`（section 端点的 `sections`，路径前缀 `sections`）/ `parseDraftSettings`（三个枚举，精确键集，路径前缀 `settings`） |
| `parse-draft.test.ts` | 形状拒绝（每个 union 分支带错键、枚举、上限、字节上限）+ 每条重算的反例（改 support_count / word_count、bound 无 refs、stance 引 C*、gap 带 refs、failed 段 attempts × reason 六组、改 verify 项、改 reads / mode / llm_sections 各键、聚合 usage 伪造成和、重跑三档、coverage 计数与 owner 错配、coverage 调用 calls / temperature）+ 绑定 brief 的反例（brief_ref 三键、删 / 增 / 换序 / 改 h2 / 改 answers、C9 / 无片段的 C6 / 别的簇的 C3 / P9 / inferred P2、product_mention none / gap_only / throughout 三向切换、askable 集合、顺序、unavailable 原因、账本四个假原因、askable 为空的五种伪造（含 codex 的 fake-deployment + tokens）、失败读数 attempted/calls/model_id/tokens 十四组（含 codex 的 provider_error 1/0）、stance 出现在非缺口角度段）+ 可信重跑凭据（目标段 status / attempts / fail_reason / tokens、聚合 tokens / calls / model_id / temperature_effective、coverageLlm）+ 指纹（真 `draftFingerprint`、elapsed_ms 不敏感、注入 hasher 收到副本且不在拒绝时调用、重盖指纹的伪造仍被抓）+ `parseDraftSections` / `parseDraftSettings` 正反例；七个 fixture 变体全过 |
| `draft-fixtures.ts` | `draftResultFixture(brief, { failSection?, skipSection?, coverage?: "unavailable", productMention?, rerun?: { previousRunId, sectionId } })`（`rerun` = section 端点语义：`DRAFT_FIXTURE_RERUN_ID`、`reran_from`、`budget_ms = SECTION_ENDPOINT_BUDGET_MS`、`llm_sections` 只反映该段一次调用；`draft-fixtures.test.ts` 断言它过 `parseDraftResult`）（前两项可给数组）经 draft-assemble.ts / validate-section.ts 同一套函数组装并盖真指纹；每段只引用 `sectionEvidenceFor` 给它的证据：双源 bound、单源 bound、有 fact 在作用域时再加只引 P* 的 bound、gap、no_claim，缺口角度那一节再加 stance（作用域内有 inferred 就引它，否则引一手 fact）；coverage 按 covered / partial / none 轮流，无 askable 时 llm_coverage 为 insufficient_evidence / 0 / 0；`fixtureCallOf(section)` = handler 为该段记的 `SectionCallMeta`；`draftBrief(options?)` = `withFingerprint(contentBriefFixture({ connected: true }))` |
