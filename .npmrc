engine-strict=true
# Make @types/* resolvable across all workspace packages (they are type-only
# and safe to hoist). Keeps per-package tsconfig `types` fields working without
# declaring @types/node in every leaf package.
public-hoist-pattern[]=@types/*
