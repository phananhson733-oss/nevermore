node_modules/
.pnpm-store/
dist/
build/
.next/
.next-e2e-mock/
.next-e2e-real/
apps/web/.next-*/
out/
coverage/
*.tsbuildinfo

# env / secrets — never commit
.env
.env.local
.env.*.local
.env.local.bak-*

# test + tooling artifacts
playwright-report/
test-results/
.vitest/
.gstack/

# OS
.DS_Store

# generated OpenAPI types are committed on purpose (contracts:check compares them),
# so they are NOT ignored.

# Local blob store runtime output (raw imports / export bundles) — never commit
.data/
**/.data/
.vercel
