# AGENTS.md

本文件适用于本仓库及其全部子目录；更深层目录存在自己的
`AGENTS.md` 时，以范围更窄的文件为准。

## 双代理协作：Codex 主责，ChatGPT Pro 外部工程师

复杂研究、架构设计、跨模块实现或独立评审可以采用 Codex + ChatGPT Pro
协作。该模式不是把任务外包：**Codex 始终是需求、代码、权限和验收的
唯一负责人**；ChatGPT Pro 是只能依据已提供材料工作的外部高级工程师。

### 职责与结论边界

- ChatGPT Pro 可以研究、设计、审阅，并提供文字方案、补丁或完整代码；
  它不得假设能访问本地仓库、私有 Git、用户账号、内部服务、运行中的
  开发环境或生产数据。
- Codex 负责理解需求、预检仓库、选择并准备安全的源码附件、分解任务、
  在内置浏览器中沟通、监控进度、追问和纠错、实际落地代码、独立验证和
  最终报告。
- 外部代理的结论、测试截图或“已完成”声明均不是验收证据。Codex 必须以
  当前源码、可复现命令输出、实际构建产物和本仓库验收标准单独判断。
- 对可独立完成且互不影响的复杂主题，应分别建立独立的 ChatGPT Pro
  对话，避免把不同架构和补丁上下文混在同一会话。数据库、OpenAPI、状态
  机等唯一合同仍只能由一个 owner 统筹合并。

### 预检与安全源码包

在第一次向 ChatGPT Pro 发出工程任务前，Codex 必须：

1. 阅读适用范围内的 `AGENTS.md`、`CLAUDE.md`、README、`package.json`、
   workspace/lockfile、相关架构文档和测试文档，识别实现边界和必跑门禁。
2. 记录当前分支、HEAD commit、`git status --short` 和与任务相关的未提交
   修改；绝不覆盖、清理、回退或丢失已有工作。
3. 创建最小但足够的 ZIP 源码包。默认只包含本次任务必要的源码、配置、
   测试、规格和匿名化 fixture；排除 `.git`、`node_modules`、构建产物、
   缓存、临时目录、数据库、运行状态、日志和浏览器状态。
4. 无例外排除 `.env*`、API key、token、私钥、证书、Cookie、会话数据和
   任何其他凭据。上传前运行仓库的密钥扫描；如仓库无适用扫描器，至少用
   明确规则扫描常见 secret 标识和私钥头，并在报告中说明局限。
5. 将源码基线 commit（未提交工作则同时说明状态）、ZIP 的文件清单、字节
   大小、SHA-256 和密钥扫描结果记录到任务证据中。未通过扫描的附件不得
   上传。

### 外部任务说明与协作纪律

给 ChatGPT Pro 的每项工程任务必须是可执行、可验收的任务书，至少包含：

- 背景、业务目标和明确的非目标；
- 当前架构、相关目录、版本/运行环境，以及不可破坏的合同和安全边界；
- 需要研究、修改和不应修改的范围；
- 所需交付物（设计结论、文件级补丁、测试、风险和假设）；
- 必须执行的测试、不得执行或不得声称执行的操作；
- 可由 Codex 复核的验收标准；
- 已上传附件的范围及其基线，而非要求它访问本地路径或私有资源。

- 为每个对话保存可恢复的链接、启动时间、任务版本和外部交付物位置。
  在页面刷新、上下文截断或连接中断后，Codex 应用已记录的链接和摘要
  自主恢复，不要求用户转交技术信息。
- 外部代理长时间工作时，不要因为运行耗时而催促、打断或重复发送任务。
  只有经过合理等待且连续检查没有进展时，才检查对话状态、重开页面，或
  要求它从已完成位置继续。
- 如外部代理交付有缺陷，Codex 应直接提供最小、可复现的反馈：命令、错误
  日志、文件/行号、违反的合同及正确约束；随后要求最小且完整的修正并
  重新验收。不得让用户充当双方的传话人。

### 独立验收、证据与权限

ChatGPT Pro 交付后，Codex 必须在应用任何方案前检查报告、补丁、源码和
附件是否完整，并独立进行下列验收：

- 核对附件基线、文件清单、大小与 SHA-256，必要时以隔离工作树应用补丁；
- 评审权限、安全边界、依赖新增、锁文件变化、代码路径和可执行流程；
- 运行本仓及改动范围要求的 formatter、lint、类型检查、unit、integration、
  contract、production build 和相关 E2E；没有运行、被跳过、Mock/fixture
  验证以及真实生产验证必须明确区分，严禁以模拟测试声称生产已验证；
- 对外部结论涉及的版本或第三方行为，查阅本地锁文件、源码或官方文档并
  记录佐证；
- 验收通过后把有价值的任务书、附件摘要、外部交付、复验日志和风险结论
  保存到仓库允许的持久目录（通常为 `docs/`），不得只留在 ChatGPT 对话
  或临时目录。

遇到登录失效、账号选择、验证码、密码、Passkey 或双因素认证时必须暂停，
通知用户亲自完成；不得索取、显示、复制、保存或上传密码、Cookie、验证码
或恢复码。

除当前用户请求明确授权外，双代理协作**不授予** commit、push、创建 PR、
部署、数据库迁移、线上配置变更、生产功能启用或真实用户数据操作的权限。
即使外部代理提出建议，也不得扩大权限。用户明确授权 commit/push 时，仍
必须先完成上述独立验收和最终 `git diff` 审查；部署和数据库迁移需要各自
单独的明确授权。

最终交付报告必须列出：外部对话链接、源码包基线与 SHA-256、实际修改、
要求外部代理修正的问题、独立测试结果、尚未验证的风险，以及代码目前是
本地修改、已提交、已推送还是已部署。

## Public Tools 系统边界

`gengrowth.ai` 的公开体验工具属于本 monorepo 内独立的 Public Tools
系统：

- 站点和 API 承载于 `apps/marketing`。
- 可复用工具契约、确定性规则和编排放在 `packages/public-tools`。
- 通用公共网络采集与 SSRF 防护放在 `packages/sources`。
- Public Tools 不得导入或依赖 `apps/web` 的认证、项目/工作区、数据库、
  队列、OpenAPI、内部服务或运行时配置。
- V0 默认匿名、无持久化、无真实用户数据写入；完整产品只能通过明确的
  CTA 跳转到 `app.gengrowth.ai`。
- 所有公开 URL 抓取必须在每次重定向前重新校验目标，并在建立连接时
  固定已校验 IP，不能只做 DNS 预检后调用普通 `fetch`。
- 对无法由当前证据证明的结论使用 `unverified`，不得把响应截断、静态
  HTML 缺失、标准路径缺失或网络失败伪装为确定性失败。

## 工作与验收

- 开始复杂任务前阅读 `CLAUDE.md`、README、`package.json`、锁文件和
  相关架构/测试文档，记录分支、commit 和 `git status`。
- 不覆盖、清理、回退或丢失用户已有修改；禁止未经授权的 commit、push、
  PR、部署、数据库迁移、线上配置或生产数据操作。
- 变更遵循 SPEC-first 和测试先行；JS/TS 修改后至少运行适用的格式化、
  lint、类型检查、单测和生产构建。
- 完成前审查 `git diff`，运行仓库要求的验证门禁；未执行、不适用、Mock
  与真实生产验证必须明确区分。
