{
  "name": "@sf/worker",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development tsx watch --env-file-if-exists=.env.local src/index.ts",
    "start": "tsx --env-file-if-exists=.env.local src/index.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  },
  "dependencies": {
    "@sf/artifacts": "workspace:*",
    "@sf/contracts": "workspace:*",
    "@sf/db": "workspace:*",
    "@sf/engine": "workspace:*",
    "@sf/observability": "workspace:*",
    "@sf/sources": "workspace:*",
    "pg-boss": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:",
    "typescript": "catalog:"
  }
}
