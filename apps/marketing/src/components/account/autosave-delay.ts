// @input  -- none
// @output -- the one debounce both editors on the account page share
// @pos    -- a single constant, so the two halves of the page settle alike
export const ACCOUNT_AUTOSAVE_DELAY_MS = 900;
