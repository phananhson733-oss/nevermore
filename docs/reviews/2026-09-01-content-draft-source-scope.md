# Content Draft provider-scope follow-up

## Finding

Independent review after PR #274 found the same semantic scope error in a full production Draft and its O2 rerun. The text first let the reader choose an unspecified free calculator, then presented Cafe Astrology's observed OK-button/no-email/no-download behavior as that generic calculator's behavior. The U54 reference was valid, but the prose widened its subject. A single-source verification warning does not cure that error, particularly after clean Markdown export removes annotations.

## Surgical correction

The Draft v2/v3 section prompt now requires source attribution in the sentence/prose for provider-specific interface steps, pricing, account/email, privacy, download and installation conditions. Use a supported service name or a plain source domain when needed; do not invent a brand or introduce a navigation URL. These details cannot be generalized to other tools or transferred to the target site's capabilities. If attribution is unavailable, omit the specific claim or make the missing verification explicit as a gap with no evidence references.

No schema, parser, model/deployment, credentials, temperature, deadline, retry count, scope IDs, or export formatter changes are included. This continues the user's existing authorization for code, PR/merge and Marketing-only production delivery.

## Verification scope

Two request-level tests were first RED, then GREEN: the rule reaches both the initial request and the existing validation-correction retry, without expanding evidence references or budgets. Native independent review and a real O2 probe use the exact previously confirmed production Brief, not newly acquired sources or a synthetic claim of publication. Fresh production validation and disk-export completion are still required before this follow-up is considered closed.

The first prompt-only candidate passed those offline checks but failed its real 12.194-second O2 probe: the output still said “the supplied instructions” without identifying the provider. It was not published. The next candidate clarifies that source attribution is not target-product promotion and exposes each selected unit's exact source domain beside its text, derived from the frozen final URL. This is private model-input metadata, not a public schema or source-scope change.

The second real O2 probe passed in 8.776 seconds with one Luna attempt and unchanged configuration. Its OK/no-download/no-email sentence explicitly begins with the exact `astro.cafeastrology.com` domain; the birth-data instructions separately identify `maressabrown.com`. The exact confirmed fingerprint, two required H3 headings, scoped U references and strict section parser remain intact. Root's related suite passed 1243/1243 across 43 files and the fresh Marketing build passed (300 routes). These local results still require exact-head release and post-deploy verification.
